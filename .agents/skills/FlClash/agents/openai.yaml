interface:
  display_name: "FlClash"
  short_description: "Repo-specific patterns and workflows for FlClash"
  default_prompt: "Use the FlClash repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true